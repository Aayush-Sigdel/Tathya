import React from 'react';
import styles from './BiasMeta.module.css';

const BiasMeta = ({ biasPercentage, sources, variant = 'default' }) => {
    return <div className={styles['bias-meta-container']}></div>;
};

export default BiasMeta;
